# 
# .zshrc
#
# シェル起動時に読み込まれる
# 

source ~/.zsh.d/zshrc

# LSの色付け
alias ls='ls --color'
alias ll='ls --color -l'
alias la='ls --color -la'


# ローカルファイルの読み込み
[ -f ~/.zshrc.local ] && source ~/.zshrc.local

# vim: set ft=zsh :

